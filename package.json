{
  "name": "ci-failure-triage",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone, vendor-neutral CI failure intelligence and triage platform",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx apps/cli/src/index.ts",
    "triage": "tsx apps/cli/src/index.ts"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
